# Environment Configuration for Runna to Intervals.icu Converter

# Required: ICS calendar URL
ICS_URL=https://your-calendar-url.com/calendar.ics

# Optional: Output file path (default: intervals_icu_workouts.json)
OUTPUT_PATH=intervals_icu_workouts.json

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Use JSON-formatted logs (true/false)
# Set to 'true' for Cloudflare Workers and structured logging environments
LOG_JSON=false

# ============================================================================
# Instructions:
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and add your actual ICS URL
#
# 3. Run locally:
#    source .env && python3 runna_to_intervals.py
#
# Or use the run script:
#    ./run.sh
#
# For Cloudflare Workers:
# Set these as secrets using wrangler:
#    echo "your-url" | wrangler secret put ICS_URL
#    echo "INFO" | wrangler secret put LOG_LEVEL
#
# ============================================================================
